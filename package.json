{
  "name": "@woltz/monorepo",
  "version": "1.0.0",
  "private": false,
  "description": "Woltz monorepo containing DDD libraries",
  "workspaces": [
    "packages/*",
    "examples/**/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@woltz/rich-domain && npm run build --workspace=@woltz/react-rich-domain && npm run build --workspaces",
    "postinstall": "npm run build",
    "test": "npm run test --workspace=@woltz/rich-domain && npm run test --workspace=@woltz/react-rich-domain",
    "test:watch": "npm run test:watch --workspace=@woltz/rich-domain",
    "check": "npm run check --workspace=@woltz/rich-domain",
    "coverage": "npm run coverage --workspace=@woltz/rich-domain",
    "lint": "npm run lint --workspace=@woltz/rich-domain && npm run lint --workspace=@woltz/react-rich-domain",
    "clean": "npm run clean --workspaces && rm -rf node_modules",
    "prepare": "husky",
    "publish:rich-domain": "npm publish --workspace=@woltz/rich-domain"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "eslint": "^9.39.1",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "standard-version": "^9.5.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
