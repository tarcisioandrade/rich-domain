name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: ["22.x"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - name: Install dependencies
        run: npm install --prefer-offline

      - name: Fix native optional deps
        run: npm install @rollup/rollup-linux-x64-gnu lightningcss-linux-x64-gnu --no-save

      - name: Run formatter
        run: npm run format:check

      - name: Build
        run: npm run build

      - name: Run linter
        run: npm run lint

      - name: Run type checking
        run: npm run check

      - name: Run tests
        run: npm test

      - name: Verify changeset for publishable packages
        if: github.event_name == 'pull_request'
        run: npx changeset status --since=origin/${{ github.base_ref }}
